// WAP to reverse the order of characters in a string

#include<stdio.h>
#include<string.h>

int main() {

    char str[100] = "My Name is Ronit Batra";
    
    printf("the reverse of the string is %s", strrev(str));
    return 0;
}